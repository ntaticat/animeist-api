# Animeist API

Esta es una API para gestionar listas de anime. Hecha con Node/Express y Mongoose (MongoDb).

## Dev environment
cp .env.example .env
npm install
docker compose up -d
npm run dev

## Swagger Ui
http://localhost:4000/api/docs/v1